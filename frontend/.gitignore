# dependencies
/node_modules

# build output (Vite) and test reports
/dist
/coverage

# local configuration — only the template is tracked
.env
.env.*
!.env.example

# Capacitor platform folders are generated by `npx cap add`
/android
/ios

# misc
.DS_Store
npm-debug.log*
