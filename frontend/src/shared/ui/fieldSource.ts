// Откуда взялось значение поля. Ради этого различия и затевалось распознавание:
// подставленное приложением поле надо перечитать, введённое руками — уже прочитано автором.
//
// Значения ровно два, как на проводе (`ocr` и `seller`): различать «из VIN» и «из СТС»
// экран не может — сервер такого не сообщает, а выведенная самим фронтом пометка
// выдавала догадку за факт.
export type FieldSource = 'manual' | 'recognized'
