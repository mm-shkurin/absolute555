// Версия опубликованного контракта.
//
// Здесь лежала карта путей, нарисованная по мокапам, — `/listings`, `/import-requests`.
// Все они получили настоящие ручки в историях 16–18, кроме ленты витрин поставщиков,
// которой на сервере нет вовсе; карта же осталась, и лента «под заказ» ходила по ней в
// `/api/v1/listings` за 404. Ответ падал в Promise.all, и раздел не открывался ни у
// кого — включая вкладку с машинами, которая работает.
//
// Правило проекта на этот случай: нерабочее — из репозитория, а не под комментарий.
// Настоящая карта живёт в `backend/paths.ts`; чего сервер не отдаёт, того экран не
// рисует, а расхождение записано в `api-specs/backend-contract-map.md`.
export const API_VERSION = 'v1'
