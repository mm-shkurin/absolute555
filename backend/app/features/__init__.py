"""Предметные области приложения. Импорт любой из них регистрирует весь граф таблиц.

Таблицы ссылаются друг на друга через границы областей — у объявления есть жалобы, у
оффера отзыв, — а SQLAlchemy разрешает связь по имени класса только после того, как
зарегистрированы все. Импорт одной области в одиночку оставил бы мапперу имена, которых
он не найдёт, и падало бы это не здесь, а в первом же запросе с selectinload.
"""

import app.db.registry  # noqa: F401
