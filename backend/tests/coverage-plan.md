# План покрытия бэкенда до >= 90%

Тесты описаны **от пользовательской истории**, а не от строчек прод-кода: строка —
следствие, история — причина. Столбец «код» отвечает на один вопрос: есть ли в
`app/` код, который этот сценарий проходит.

Легенда столбца «код»:

- `+` — прогон зелёный;
- `-` — прогон красный, код надо править (тест помечен `xfail(strict=True)`).

**Прогон 12.09.2026: 75% покрытия, 629 тестов, 3 обвинения коду.**

Прогон: `cd infra && make test` (нужен Postgres, Redis, MinIO — см.
`.github/workflows/backend.yml`), покрытие — `pytest --cov=app --cov-report=term-missing`.

## Что уже покрыто (65 файлов, 512 тестов)

Ленты и карточка, жизненный цикл объявления, галерея, СТС и толщиномер, офферы,
чат и непрочитанное, модерация очереди и жалоб, роли и заявки на роль, профиль
поставщика, заявки покупателя, рейтинг продавца, OAuth Яндекса, каталог, гости.
Эти области ниже не дублируются — перечислены только дыры.

## Дыры и новые тесты

### Продавец правит и снимает объявление — `test_listing_edit.py`

| # | История | Проверка | Код |
|---|---|---|---|
| E1 | Продавец правит цену у черновика | `PATCH /sale_car/{id}` возвращает новое значение | + |
| E2 | Продавец шлёт пустой патч | 422/400 `EMPTY_PATCH`, ничего не изменилось | + |
| E3 | Чужой человек правит объявление | 403/404, значение не изменилось | + |
| E4 | Патч с неизвестным полем (`status`) | отказ схемы, статус не подменён | + |
| E5 | Продавец удаляет своё объявление | 204, затем 404 при чтении | + |
| E6 | Чужой удаляет объявление | отказ, объявление на месте | + |
| E7 | Настройки видимости (`phone_visible` и т.п.) правятся как поля | ответ отражает выбор | + |

### Покупатель раскрывает телефон — `test_reveal_phone.py`

| # | История | Проверка | Код |
|---|---|---|---|
| P1 | Телефона нет в карточке до нажатия | в `GET /sale_car/{id}` номер скрыт для чужого | + |
| P2 | Вошедший нажимает «показать номер» у опубликованного | 200 и номер | + |
| P3 | Аноним нажимает то же | 401 | + |
| P4 | Объявление в черновике | 404 (а не «нет номера») | + |
| P5 | У объявления номер не заполнен | 404, тот же ответ, что и у скрытого | + |

### Продавец вписывает VIN руками — уже покрыто

`test_vin_decode.py` (10 тестов) держит и приём VIN, и отказ по форме, и чужого, и
объявление на модерации. Написанный было `test_vin_decode_api.py` дублировал его целиком
и удалён.

### Непрочитанное и прямой диалог — `test_chat_unread.py`

| # | История | Проверка | Код |
|---|---|---|---|
| C1 | Значок непрочитанного у нового пользователя | `GET /chat/unread` = 0 | + |
| C2 | Пришло письмо — значок вырос | unread == числу чужих сообщений | + |
| C3 | Свои сообщения непрочитанными не считаются | автор видит 0 | + |
| C4 | Прочитал — значок обнулился | `POST /dialogs/{id}/read` даёт `unread: 0` | + |
| C5 | Отметка чужих идентификаторов сообщений | не роняет ответ, `marked` честный | + |
| C6 | Страницы истории | `page`/`size`, `total`, границы (`size=0`, `size=101`) | + |
| C7 | Чужой диалог | 404, а не 403 | + |
| C8 | Прямой диалог с поставщиком повторно | тот же `dialog_id`, второго не создаётся | + |
| C9 | Прямой диалог с несуществующим человеком | 404 | + |

### Модератор смотрит на нагрузку — `test_moderation_counts.py`

| # | История | Проверка | Код |
|---|---|---|---|
| M1 | Счётчики вкладок | `GET /moderation/counts` — ключи вкладок, числа >= 0 | + |
| M2 | Отправленное на проверку увеличило `waiting` | разница ровно 1 | + |
| M3 | Одобренное ушло из `waiting` | счётчик вернулся | + |
| M4 | Вкладка «сегодня решённые» | одобренное объявление там видно | + |
| M5 | Границы страницы очереди | `size=61` и `tab=whatever` отбиты 422 | + |
| M6 | Обычный пользователь просит счётчики | 403 | + |
| M7 | Снятие с публикации без причины | 422; с причиной — статус сменился | + |

### Администратор ищет человека — `test_admin_users_page.py`

| # | История | Проверка | Код |
|---|---|---|---|
| A1 | Страница списка | `total`, `page`, `size`, длина `items` <= size | + |
| A2 | Фильтр по роли | все выданные — с этой ролью | + |
| A3 | Фильтр «только закрытые» | заблокированный виден, обычный нет | + |
| A4 | Поиск по имени | найден переименованный пользователь | + |
| A5 | Границы страницы | `page=0`, `page_size=101` отбиты 422 | + |
| A6 | Карточка роли | `role-info` отдаёт текущую роль | + |
| A7 | Роль несуществующего | 404 `USER_NOT_FOUND` | + |
| A8 | Сводка по ролям | `/role/stats`: сумма по ролям == total | + |
| A9 | Обычный пользователь просит список/сводку | 403 на всех четырёх ручках | + |

### Документация закрыта ключом — `test_docs_gate.py`

| # | История | Проверка | Код |
|---|---|---|---|
| D1 | Страница входа в Swagger отдаётся | 200, форма на `/docs/auth` | + |
| D2 | Неверный ключ | 401, схема не отдана | + |
| D3 | Верный ключ | 302 на `/docs/swagger`, кука выставлена httponly | + |
| D4 | Swagger без куки | редирект на `/docs`, а не 200 | + |
| D5 | `GET /openapi.json` без куки | схема не отдана | + |
| D6 | `GET /openapi.json` с кукой | схема с описанными путями | + |
| D7 | То же для ReDoc | `/redoc`, `/redoc/auth`, `/redoc/view` | + |

### Покупатель читает отклики — уже покрыто

`test_buyer_requests.py` держит все семь сценариев (свои отклики, чужой читатель,
идемпотентный повтор, закрытая заявка, закрытие не автором, лента спроса покупателю,
счётчик откликов). Новых тестов не нужно — строка оставлена, чтобы дыру не «нашли»
второй раз.

### Человек уходит с площадки — уже покрыто

`test_account_profile.py`: удаление, закрытый вход, оставшиеся следы, снятие аватара,
не-картинка, пустое имя. Дублировать нечего.

### Витрины поставщиков — `test_supplier_storefronts.py`

| # | История | Проверка | Код |
|---|---|---|---|
| S1 | Гость смотрит ленту витрин | одобренная витрина в `/supplier` | + |
| S2 | Страница ленты | `page`, `size`, `total` честные | + |
| S3 | Границы страницы | `page=0`, `size=0`, `size=61` отбиты 422 | + |
| S4 | Ожидающая витрина | в ленту не попала | + |
| S5 | Отклонённая витрина | ушла из ленты, публично 404 | + |
| S6 | Одобрение неподанного профиля | отказ, витрина не появилась | + |
| S7 | Очередь и решение обычному пользователю | 403 на обоих | + |
| S8 | Витрина несуществующего человека | 404 | + |

### Сроки токенов и сессия документации — `test_token_expiry.py`

| # | История | Проверка | Код |
|---|---|---|---|
| T1 | Просроченный access-токен | 401, а не 500 | + |
| T2 | Просроченный refresh | 401 | + |
| T3 | Токен без поля «тип» | 401 | + |
| T4 | Refresh, подписанный ключом access | 401 | + |
| T5 | Выход с просроченным токеном | тот же 204 | + |
| T6 | Refresh несуществующего человека | 401/404 | + |
| T7 | Сессия документации | валидна только выданная; снятие идемпотентно | + |

> Замечено по ходу: `session_service` держит сессии в множестве на уровне модуля —
> состояние в памяти процесса. При нескольких uvicorn-воркерах кука, выданная одним,
> невалидна у другого, а перезапуск разлогинивает всех. Это нарушение правила из
> `.claude/rules/coding-rules.md` («Deployment»); тест T7 намеренно проверяет
> `SessionService` как класс, а не глобальный экземпляр, чтобы не цементировать
> синглтон.


## Обвязка: то, что не видно ни с одного экрана

Эти модули HTTP-тесты задевают по касательной — ветки отказов и вырожденные входы в них
не попадают. Проверяются напрямую, без базы.

### Матрица прав и запреты гостю — `test_permission_matrix.py`

| # | История | Проверка | Код |
|---|---|---|---|
| PM1 | Админ может всё | `ROLE_PERMISSIONS[ADMIN] == set(Permission)` | + |
| PM2 | Менеджер не трогает роли и журнал | `MANAGE_ALL_USERS`, `VIEW_ACCOUNT_AUDIT` вне набора | + |
| PM3 | Поставщик = пользователь + одно право | разница ровно `MANAGE_SUPPLIER_PROFILE` | + |
| PM4 | У гостя два права | своё чтение и загрузка файлов | + |
| PM5 | Каждая роль описана | в матрице есть все значения `UserRole` | + |
| PM6 | Проверка разрешает и отказывает | `can`/`assert_can`, `details.required` в отказе | + |
| PM7 | Неизвестная роль | ничего не может, а не всё | + |
| PM8 | Гость на каждой заставе | три `forbid_*` дают `GUEST_FORBIDDEN` | + |

### Живая доставка чата — `test_chat_hub.py`

| # | История | Проверка | Код |
|---|---|---|---|
| H1 | Сообщение обеим сторонам | оба слушателя получили | + |
| H2 | Две вкладки одного человека | обе получили | + |
| H3 | Третий не слышит | очередь пуста | + |
| H4 | После отключения не доставляется | очередь пуста | + |
| H5 | Отключение того, кто не подключался | без падения | + |
| H6 | Очередь переполнена | потеря строки, не падение процесса | + |
| H7 | Токен превращается в человека | id из access-токена, с `Bearer` и без | + |
| H8 | Всё остальное — никто | пусто, чужой ключ, refresh, просрочка, мусор | + |

### Поток статусов распознавания — `test_sse_fanout.py`

| # | История | Проверка | Код |
|---|---|---|---|
| F1 | Подписчик объявления получает статус | очередь непуста | + |
| F2 | Подписчик другого объявления — нет | очередь пуста | + |
| F3 | Две вкладки | обе получили | + |
| F4 | После закрытия вкладки | не доставляется, ключ удалён | + |
| F5 | Отписка неподписанного | без падения | + |
| F6 | Никто не слушает | отправка не падает | + |

### Запись распознанного — `test_decode_persist.py`

| # | История | Проверка | Код |
|---|---|---|---|
| DP1 | Полное чтение | VIN, год, мощность, коробка на месте | + |
| DP2 | Номер кузова | пишется в своё поле, VIN не трогается | + |
| DP3 | Пустое не затирает введённое руками | старые значения целы | + |
| DP4 | Число строкой | `"2018"` становится 2018 | + |
| DP5 | Не-число | поле не меняется, задача не падает | + |
| DP6 | Марка и модель | здесь не пишутся — это дело справочника | + |

### Ответ модели в поля — `test_model_answer.py`

| # | История | Проверка | Код |
|---|---|---|---|
| MA1 | Чистый JSON | поля разобраны | + |
| MA2 | JSON внутри болтовни | найден | + |
| MA3 | Лишние поля | отброшены | + |
| MA4 | Забытое поле | пусто | + |
| MA5 | Не-JSON, пусто, `None` | все поля пустые, без исключения | + |
| MA6 | «не указано», «null», «-» | читается как пусто | + |

### Настройки — `test_config_settings.py`

| # | История | Проверка | Код |
|---|---|---|---|
| CF1 | Строка подключения | собирается из частей, хост по умолчанию localhost | + |
| CF2 | Порт вне диапазона | отказ схемы | + |
| CF3 | Пустой логин/пароль/база | отказ схемы | + |
| CF4 | Короткий ключ подписи | отказ схемы | + |
| CF5 | Настройки живого `.env` | читаются, порты и бакет заданы | + |

## Чего ещё нет

Дальнейшая добивка возможна только по отчёту `--cov-report=term-missing`: угадывать,
какие ветки в `listing_feed`, `offer_service`, `review_service` и читалках СТС остались
белыми, дешевле после первого прогона, чем до него. Кандидаты, которые уже видно:
`s3_bucket`, `cache_service.take`, `catalog_resolver`, `webhook_service`,
`listing_stream`, `status_updater`.



### Читалки и обвязка — второй заход

| Файл | Что закрывает |
|---|---|
| `test_sts_image.py` | подготовка кадра СТС: ужать снимок с телефона, растянуть миниатюру, EXIF-поворот, RGBA/L/P в RGB, три кандидата в пределах потолка |
| `test_sts_number.py` | сведение двух чтений номера: согласие, спор, кириллические двойники, номер кузова, явное «ОТСУТСТВУЕТ» |
| `test_cache_take.py` | `take` как «один раз значит один раз», TTL, батч, кэш без Redis |
| `test_listing_stream.py` | поток статусов: кривой id, первый кадр, чужое сообщение, отписка |
| `test_queue_jobs.py` | обе задачи очереди по последовательностям статусов |
| `test_webhook_announce.py` | три вебхука и то, что недоступный канал не роняет публикацию |
| `test_ownership.py` | владение объявлением и предложением, три формы `require_*` |

Настоящие снимки СТС не понадобились: `prepare_candidates` не понимает, что на кадре, —
она приводит любой кадр к тому, что переваривает распознавание, и это проверяется
синтетикой. Живые документы нужны только для качества чтения, а его меряет
`scripts/gauge_eval.py`, а не набор тестов.

## Найденные баги — починены

Тесты были красными под `xfail(strict=True)`, пока баги стояли. Код исправлен,
маркеры сняты, тесты держат правило дальше.

| # | Где | Что не так | Тест |
|---|---|---|---|
| B1 ✅ | `chat_service.open_direct` | Собеседник не проверялся: внешний ключ падал `IntegrityError`, наружу уходило 500. Теперь строка человека читается до вставки | `test_chat_unread.py::test_should_refuse_a_direct_dialogue_with_nobody` |
| B2 ✅ | `security.refresh_access_token` | Проверялась подпись, но не человек: ушедшая запись обновляла токены бесконечно. Добавлена `_still_allowed` — нет, удалён или закрыт получает отказ | `test_token_expiry.py::test_should_refuse_a_refresh_token_of_a_user_who_never_existed` |
| B3 ✅ | `PeopleService._conditions` | Поиск шёл по `yandex_json` и `vk_json` мимо колонки `profile_name` — переименовавшегося консоль не находила. Ищет по трём местам | `test_admin_users_page.py::test_should_find_a_person_by_name` |

Мёртвый код удалён: `create_sale_car`, `get_all_sale_cars` и `update_vin` в
`listing_records` не вызывал никто (файл со 123 строк до 80), закомментированные
тройными кавычками `DELETE /offer/{id}` и `/{id}/with-details` убраны из роутера —
он читался так, будто ручки есть.

Наблюдения без теста:
- Снять одобренную витрину поставщика нечем: `reject` работает только по ожидающему
  профилю, для опубликованного отвечает `SUPPLIER_NOT_FOUND`. У объявлений для этого есть
  `/moderation/listings/{id}/unpublish`, у витрин — ничего.
- `session_service` держит сессии документации в множестве на уровне модуля.

## Порядок работы

1. ~~Написать тесты.~~ Сделано.
2. ~~Поднять стек и прогнать.~~ Сделано: 75%, 629 тестов.
3. ~~Разобрать падения.~~ Сделано: 7 были ошибками тестов (исправлены), 3 — баги кода (B1-B3).
4. Починить B1-B3 и снять `xfail`.
5. Добить с 75% до 90% по `term-missing`. Самые крупные белые пятна:

| Модуль | Покрытие | Что не выполняется |
|---|---|---|
| `ml/sts_image.py` | 8% | подготовка снимка СТС — тесты идут мимо, на готовых кадрах |
| `ml/sts_ocr.py` | 10% | распознавание текста документа |
| `sse/listing_stream.py` | 11% | поток статусов: подписка, heartbeat, разрыв |
| `tasks/decode_vin.py` | 21% | задача чтения СТС целиком |
| `tasks/decode_by_vin.py` | 29% | задача расшифровки по VIN |
| `webhook_service.py` | 36% | приём отчёта распознавания |
| `review_service.py` | 40% | ветки отказов отзыва |
| `offer_service.py` | 44% | переходы статусов предложения |
| `listing_records.py` | 39% | чтение и фильтры объявлений |
| `complaint_service.py` | 46% | группировка и разбор жалоб |

Порядок добивки: сначала сервисы (`offer`, `review`, `complaint`, `sale_cars`) — они
поведение, а не обвязка, и закрываются HTTP-тестами; затем задачи и поток; читалки СТС
последними — им нужны настоящие снимки, и там честнее оставить пробел, чем городить
моки на 100 строк подготовки изображения.
