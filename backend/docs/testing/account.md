# Учётная запись

Чтение своего профиля, смена имени, аватар и удаление учётной записи. Окружение, учётные записи
и формат ошибки — в [README.md](README.md).

### TC-ACC-001 — Профиль гостя

| Поле | Значение |
|---|---|
| ID | TC-ACC-001 |
| Название | Профиль гостя |
| История | 01 — Вырезать сервисный контур из бэкенда |
| Описание | Вошедший видит свою запись; гость отмечен флагом `is_guest` |
| Приоритет | High |
| Предусловия | GUEST_G выполнил вход |
| Тестовые данные | — |
| Шаги | 1. GET /user/profile, Authorization: Bearer <TOKEN_GUEST_G> |
| Ожидаемый результат | `200`; `is_guest` = true, `user_type` = `guest`, `device_id` = `qa-guest-g`; есть поля `id`, `name`, `avatar_url`, `role`, `is_verified`, `created_at` |
| Статус | Not run |
| Фактический результат | |

### TC-ACC-002 — Своё имя сохраняется и показывается в профиле

| Поле | Значение |
|---|---|
| ID | TC-ACC-002 |
| Название | Своё имя сохраняется и показывается в профиле |
| История | 21 — Аккаунт: имя, фотография, выход, удаление |
| Описание | Выбранное имя перекрывает имя провайдера |
| Приоритет | High |
| Предусловия | SELLER_A выполнил вход |
| Тестовые данные | body `{"name":"Пётр Кузнецов"}` |
| Шаги | 1. PATCH /user/profile, Authorization: Bearer <TOKEN_SELLER_A>, Content-Type: application/json, body {"name":"Пётр Кузнецов"}<br>2. GET /user/profile, Authorization: Bearer <TOKEN_SELLER_A> |
| Ожидаемый результат | Шаг 1: `200`, `name` = `Пётр Кузнецов`; тело содержит `id`, `name`, `avatar_url`, `role`, `is_guest`, `is_verified`, `rating`, `reviews_count`, `deals_count`, `created_at`<br>Шаг 2: `name` = `Пётр Кузнецов` |
| Статус | Not run |
| Фактический результат | |

### TC-ACC-003 — Пустое имя возвращает имя провайдера

| Поле | Значение |
|---|---|
| ID | TC-ACC-003 |
| Название | Пустое имя возвращает имя провайдера |
| История | 21 — Аккаунт: имя, фотография, выход, удаление |
| Описание | Строка из пробелов сбрасывает своё имя; у гостя имени провайдера нет |
| Приоритет | Medium |
| Предусловия | Гость с `device_id` `qa-acc-003` выполнил вход; имя задано, как в TC-ACC-002 |
| Тестовые данные | body `{"name":"   "}` |
| Шаги | 1. PATCH /user/profile, Authorization: Bearer <ACCESS>, body {"name":"   "} |
| Ожидаемый результат | `200`, `name` = null |
| Статус | Not run |
| Фактический результат | |

### TC-ACC-004 — Имя длиннее 60 символов и лишнее поле отклоняются

| Поле | Значение |
|---|---|
| ID | TC-ACC-004 |
| Название | Имя длиннее 60 символов и лишнее поле отклоняются |
| История | 21 — Аккаунт: имя, фотография, выход, удаление |
| Описание | Длина имени ограничена 60 символами; контракт правки не принимает других полей |
| Приоритет | Medium |
| Предусловия | SELLER_A выполнил вход |
| Тестовые данные | Имя из 61 символа `и`; body `{"name":"A","role":"admin"}` |
| Шаги | 1. PATCH /user/profile, Authorization: Bearer <TOKEN_SELLER_A>, body {"name":"<61 символ>"}<br>2. PATCH /user/profile, Authorization: Bearer <TOKEN_SELLER_A>, body {"name":"A","role":"admin"} |
| Ожидаемый результат | Шаги 1–2: `422`, `code` = `VALIDATION_ERROR`, в `details.errors[]` указано поле (`name` / `role`) |
| Статус | Not run |
| Фактический результат | |

### TC-ACC-005 — Правка профиля без токена

| Поле | Значение |
|---|---|
| ID | TC-ACC-005 |
| Название | Правка профиля без токена |
| История | 21 — Аккаунт: имя, фотография, выход, удаление |
| Описание | Анонимный вызов отклоняется до обращения к данным |
| Приоритет | High |
| Предусловия | Стек поднят |
| Тестовые данные | body `{"name":"кто-то"}` |
| Шаги | 1. PATCH /user/profile, Content-Type: application/json, body {"name":"кто-то"}, без Authorization<br>2. DELETE /user, без Authorization |
| Ожидаемый результат | Шаги 1–2: `401`, `code` = `CREDENTIALS_INVALID` |
| Статус | Not run |
| Фактический результат | |

### TC-ACC-006 — Загрузка и снятие аватара

| Поле | Значение |
|---|---|
| ID | TC-ACC-006 |
| Название | Загрузка и снятие аватара |
| История | 21 — Аккаунт: имя, фотография, выход, удаление |
| Описание | Загруженная фотография видна в профиле; снятие возвращает `avatar_url` = null |
| Приоритет | High |
| Предусловия | SELLER_A выполнил вход; MinIO поднят |
| Тестовые данные | Файл `face.png` — настоящее PNG-изображение до 10 МБ |
| Шаги | 1. PUT /user/avatar, Authorization: Bearer <TOKEN_SELLER_A>, multipart/form-data, поле `file` = face.png (image/png)<br>2. GET /user/profile, Authorization: Bearer <TOKEN_SELLER_A><br>3. DELETE /user/avatar, Authorization: Bearer <TOKEN_SELLER_A> |
| Ожидаемый результат | Шаг 1: `200`, `avatar_url` не пустой<br>Шаг 2: `avatar_url` равен значению из шага 1<br>Шаг 3: `200`, `avatar_url` = null |
| Статус | Not run |
| Фактический результат | |

### TC-ACC-007 — Файл, не являющийся изображением, не принимается как аватар

| Поле | Значение |
|---|---|
| ID | TC-ACC-007 |
| Название | Файл, не являющийся изображением, не принимается как аватар |
| История | 21 — Аккаунт: имя, фотография, выход, удаление |
| Описание | Проверяется содержимое файла, а не заявленный тип |
| Приоритет | Medium |
| Предусловия | SELLER_A выполнил вход |
| Тестовые данные | Файл `face.png` с содержимым `not a picture at all`, тип image/png |
| Шаги | 1. PUT /user/avatar, Authorization: Bearer <TOKEN_SELLER_A>, multipart/form-data, поле `file` = face.png |
| Ожидаемый результат | `422`, `code` = `NOT_AN_IMAGE`, `details.filename` = `avatar` |
| Статус | Not run |
| Фактический результат | |

### TC-ACC-008 — Удаление учётной записи закрывает вход

| Поле | Значение |
|---|---|
| ID | TC-ACC-008 |
| Название | Удаление учётной записи закрывает вход |
| История | 21 — Аккаунт: имя, фотография, выход, удаление |
| Описание | Удалённая запись неотличима от несуществующей; прежний токен не работает |
| Приоритет | High |
| Предусловия | Гость с новым `device_id` `qa-acc-008` выполнил вход (не использовать именованные записи) |
| Тестовые данные | `<ACCESS>` этого гостя |
| Шаги | 1. DELETE /user, Authorization: Bearer <ACCESS><br>2. GET /user/profile, Authorization: Bearer <ACCESS> |
| Ожидаемый результат | Шаг 1: `204`, без тела<br>Шаг 2: `401`, `code` = `CREDENTIALS_INVALID` |
| Статус | Not run |
| Фактический результат | |
