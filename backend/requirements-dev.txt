# Test-only dependencies. Never installed into the runtime image — see
# infra/docker/backend.Dockerfile, which copies requirements.txt alone.
-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.25.0

# Starlette 0.27's TestClient calls httpx.Client(app=...), a shortcut httpx dropped in
# 0.28. Runtime pins httpx==0.28.1, so tests pin it back down; this line must go when
# starlette moves past 0.28's removal.
httpx==0.27.2
