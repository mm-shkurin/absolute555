# Правки от 2026-09-06 — рабочий план

Тринадцать правок с тестового прогона по стенду `c1262dc`. **Все приняты к исполнению.**
Обсуждению подлежит только способ, не необходимость: если что-то в интерфейсе не
нравится человеку, который им пользуется, — это дефект, и объяснять его не надо.

Документ написан для исполняющей сессии: по каждой правке названо, что должно стать,
какие файлы это трогает, что уже проверено и на что там наступишь. Разбор причин, где он
что-то меняет в способе, оставлен; там, где я предлагал спросить человека, решение уже
принято и записано — сессия не должна на нём буксовать.

Состояние на входе: ветка `features/thickness-map`, слита в `dev` коммитом `c1262dc`.
Стенд поднят на `localhost`, база очищена от объявлений 2026-09-05, в ней одно тестовое
объявление, 13 витрин поставщиков, 73 заявки покупателей.

---

## Как работать по этому плану

Полоса фронтовая, кроме правок 5 и 11-в — там нужен и сервер. Конвейер прежний:
`/frontend-delivery`, гейты `bash scripts/ci-local.sh --frontend`, коммит путями без
`git add`. Сборка образа — только с `DOCKER_BUILDKIT=0`: BuildKit спотыкается о
синхронизацию OneDrive на этом дереве.

**Правки 1, 2, 3, 13 меняют дизайн, а не только код.** По правилу конвейера сначала
мокап (`ProductSpecification/ui/mockups/index.html`), потом перенос в компоненты: правка
в мокапе стоит минуты, та же правка после привязки к API — день.

Каждая пачка — отдельный коммит с прогоном гейтов. Пачки независимы, порядок между ними
можно менять, порядок внутри — нет.

---

## Пачка A. Картинки, которые есть, но не показаны

Три правки с одной причиной: компоненты рисуют заглушки, хотя данные на проводе есть.
Самая дешёвая пачка, делать первой.

### A1 — правка 10: обложка своей машины

**Стало:** в строке «Мои объявления» — превью первой фотографии; серая плашка остаётся
только у объявления без единого кадра.

**Где:**
- `frontend/src/features/myListings/api/myListingsApi.ts` — `MyListingWire` не несёт
  адреса обложки вовсе; добавить `preview_url: string | null` и переносить в
  `toMyListing` из `car.preview_photo_url`.
- `frontend/src/features/myListings/logic/myListingRows.ts` — прокинуть в `MyListingRowView`.
- `frontend/src/features/myListings/components/MyListingRow.tsx:24` — вместо
  `<Placeholder>обложка</Placeholder>` картинка, заглушка в `else`.

**Проверено:** сервер отдаёт `preview_photo_url`, ссылка открывается `200 image/jpeg`.

**Как убедиться:** чистый тест на `toMyListingRow` — обложка переносится, у объявления
без фото `null`. В браузерном сценарии `myListings` — `img` в первой строке.

### A2 — правка 11-а: обложка машины и аватар собеседника в чате

**Стало:** в списке диалогов — аватар собеседника, в шапке переписки — превью машины.

**Где:**
- `frontend/src/features/chats/components/DialogList.tsx:24` — пустой
  `<span className={styles.avatar} />` заменить на общий `Avatar` с `url`.
- `frontend/src/features/chats/components/Conversation.tsx:46` —
  `<Placeholder>фото</Placeholder>` заменить на превью объявления.
- `frontend/src/features/chats/logic/conversation.ts` — довести оба поля до вида;
  проверить, что `chat_view` на сервере их отдаёт (блок `seller` и карточка объявления),
  и если чего-то нет — записать в `api-specs/backend-contract-map.md`, а не выдумывать.

**Осторожно:** `Avatar` уже умеет и картинку, и заглушку (`shared/ui/Avatar.tsx`) —
второй компонент заводить не нужно.

### A3 — правка 6: аватар в навбаре

**Стало:** в шапке — фотография вошедшего; без фотографии прежний серый круг.

**Где:**
- `frontend/src/shared/ui/SiteHeader.tsx:82` — сейчас пустая ссылка
  `<Link className={styles.avatar} />`; отрисовать внутри `Avatar` с `url`.
- Источник адреса — `currentSession()?.avatarUrl`. Поле в `Session` есть
  (`shared/session/authSession.ts`), но при входе не заполняется: дописать в месте, где
  сессия заводится после `oauth/exchange` и гостевого входа.
- После смены фотографии в профиле сессию надо обновлять, иначе шапка отстанет до
  перезагрузки: `useProfileIdentity` уже перечитывает профиль — там же обновить сессию.

---

## Пачка B. Отступы и плотность

Четыре правки, все — вёрстка. Делать одной задачей: разрозненно они вернутся.

### B1 — правка 7: отступ перед «Кабинетом модератора»

`frontend/src/features/profile/profile.module.css:156` — у `.moderationEntry` только
`margin-bottom: 14px`. Правильное решение не «дописать `margin-top`», а завернуть блок в
общий `Panel`: у него отступы задаются одним правилом (`Panel.module.css:6`), и весь
класс подобных расхождений исчезает.

### B2 — правка 8: отступы в профиле вообще

Сейчас вертикальный ритм задан четырьмя способами: `gap: 12px` у плиток, `gap: 14px` у
шапки, `margin-top: 16px` у панелей, `margin-bottom: 14px` у входа в кабинет. Свести к
двум значениям: **16 px между секциями, 12 px внутри секции**. Значение 14 убрать
полностью — оно ниоткуда.

### B3 — правка 9: разделение обложек

`Gallery.module.css` — в полноэкранной полосе `.strip` кадры разделены только
промежутком 8 px и на тёмном холсте сливаются в ленту. Промежуток 12 px, рамка 2 px у
текущего кадра, затемнение у остальных.

### B4 — правка 12: «Показать 1 объявление» вплотную

`frontend/src/features/feed/components/FilterPanel.module.css:130` — кнопка растянута на
всю ширину колонки 236 px, надпись переносится на две строки и упирается в края.
Задать `min-height: 44px` и `line-height: 1.25`; если после этого текст всё ещё в две
строки — сократить надпись до «Показать · 1».

---

## Пачка C. Галерея и карточка

### C1 — правка 2: размер главного кадра и листание

**Стало:** кадр не выше половины экрана; листается стрелками на десктопе, свайпом на
телефоне, клавишами ← →.

**Где:** `frontend/src/features/listing/components/Gallery.tsx` и `Gallery.module.css`.

**Что именно:**
- `.main` — сейчас `aspect-ratio: 4 / 3` без потолка, при ширине колонки 1230 px даёт
  высоту ≈ 920 px. Добавить `max-height: min(56vh, 620px)`, картинке — `object-fit: contain`.
- В `Gallery.tsx:18` главный кадр — одна кнопка с единственным действием «открыть
  полноэкранный». Переключать кадр можно только щелчком по миниатюре (строка 27).
  Добавить стрелки поверх кадра и обработчики `pointerdown/pointermove/pointerup`.
- Порог свайпа ≈ 40 px по горизонтали, и только если горизонтальное смещение больше
  вертикального — иначе перехватится прокрутка страницы.

**Проверка:** браузерный сценарий на десктопе — стрелка меняет кадр; на ширине телефона
(`PHONE` в `e2e/driver.ts`) — свайп меняет кадр. Свайп в Selenium делается
`driver.actions().dragAndDrop()` либо синтетическими `pointer`-событиями через
`executeScript` — второе надёжнее, как уже сделано с выбором файла.

### C2 — правка 3: полноэкранный просмотр режет фотографию

**Стало:** кадр целиком, без обрезки, вписан в экран.

**Где:** `Gallery.module.css:84` — `.stageShot` задаёт `aspect-ratio: 4 / 3`, а картинка
обрезается `object-fit: cover` (строка 13). Вертикальное фото с телефона режется: на
присланном снимке у машины срезаны крыша и колёса.

**Что именно:** пропорцию не навязывать — `max-width: 92vw`, `max-height: 82vh`,
`object-fit: contain`, тёмный холст под кадром. `Placeholder` внутри `.stageShot` в
полноэкранном режиме не нужен: он держит рамку и фон под изображением.

### C3 — правка 1: карточка в ленте слишком большая

**Стало:** в первый экран попадают два ряда карточек.

**Где:** `frontend/src/features/feed/components/FeedStates.module.css` — сетка
`repeat(4, 1fr)` (строка 28), обложка `aspect-ratio: 4 / 3` (строка 64). Сейчас карточка
≈ 640 px высотой.

**Что именно:** обложка `16 / 10`, вертикальные отступы 16 → 12, заголовок на ступень
меньше. **Сначала в мокапе** — это пропорция, её надо увидеть, а не вычислить.

---

## Пачка D. Правки, где принято решение

Здесь я предлагал спросить человека. Решения приняты, чтобы сессия не останавливалась;
если человек скажет иначе — переделать дешевле, чем ждать.

### D1 — правка 13: марка и модель сливаются

**Проверено:** `frontend/src/features/feed/components/BrandSheet.tsx:50` и `:65` выводят
марки и модели **двумя одинаковыми `div.brandList` подряд** — один стиль кнопок, ни
заголовка, ни разделителя. После выбора марки модели просто дописываются снизу.

**Решение: шаг за шагом.** Выбрал марку — список марок заменяется списком моделей,
сверху строка возврата «‹ Volkswagen». Причина выбора: одинаково работает на телефоне и
на десктопе, а мастер продажи уже устроен шагами — приём человеку знаком. Два столбца
отвергнуты: на телефоне они всё равно схлопываются в ту же простыню.

**Проверка:** чистый тест на состояние выбора; браузерный — выбрал марку, список моделей
показан, марок не видно, возврат работает.

### D2 — правка 4: одобренный поставщик не понимает, что дальше

**Проверено:** у аккаунта роль `importer` есть, строки в `supplier_profiles` нет — витрины
он не заполнял. Одобрение выдаёт роль; витрина отправляется на проверку отдельно
(`PUT /supplier/me`, затем `POST /supplier/me/submit`). Экран об этом не говорит ни
слова, и человек ждёт появления в ленте того, чего не создавал.

**Решение: показать состояние витрины в профиле.** Блок «Ваша витрина» появляется у
роли `importer` и несёт одно из четырёх состояний с действием:

| Состояние | Что видно | Действие |
|---|---|---|
| витрины нет | «Витрина не заполнена — покупатели вас не найдут» | «Заполнить витрину» |
| `draft` | «Черновик витрины» | «Продолжить и отправить» |
| `pending` | «Витрина на проверке» | — |
| `published` | «Витрина опубликована» | «Открыть» / «Править» |
| `rejected` | причина отказа | «Исправить и отправить» |

Данные все есть: `GET /supplier/me` отдаёт статус и причину отказа.

### D3 — правка 5: отклик не ведёт в чат, заявка не закрывается

**Проверено:** `backend/app/features/importing/services/request_service.py:86` — отклик
пишет строку в `supplier_responses` и заканчивается. Диалога не заводит, статус заявки не
трогает. Глубже: таблица `dialogs` ссылается на `sale_car` — **переписки по заявке не
существует как сущности**, поэтому «перекинуть в чат» сегодня технически некуда.

**Решение: строим диалог по заявке.** Это отдельная история с бэкендовой половиной, а не
правка на ходу. Состав:

1. **Сервер.** Диалог получает вторую привязку — к заявке (`buyer_request_id`), рядом с
   существующей привязкой к объявлению. Одна из двух заполнена, обе сразу — никогда.
   Миграция, правило в сервисе чата, ручка «написать по заявке».
2. **Кто начинает.** Переписку открывает **отклик поставщика**: он уже назвал цену и
   срок, и покупателю нужно место, где спросить подробности. Приглашения от покупателя
   ждать не надо — иначе отклик остаётся строкой, на которую некому ответить.
3. **Заявка остаётся открытой.** Один отклик не значит, что покупатель нашёл машину;
   закрывает её по-прежнему только автор (`POST /request/{id}/close`). Это не дефект, и
   менять не нужно.
4. **Экран.** После отклика поставщика перекидывает в диалог; у покупателя отклик в
   списке несёт кнопку «перейти в переписку».

**Порядок:** интервью не нужно (решение выше), нужна спека — `api-specs/chat.yaml` плюс
`importing.yaml`, затем бэкенд и фронт параллельно.

---

## Пачка E. Отдельно, не срочно

### E1 — правка 11-б: чат по вебсокету

**Что известно:** `frontend/src/shared/api/backend/chatSocket.ts:21` поднимает соединение,
токен уходит **параметром адреса** (`paths.ts:83`), переподключения при разрыве в коде
нет ни одного.

**Чего не хватает:** что именно наблюдалось — сообщения не приходят, приходят дважды,
теряются после спящего режима. Без этого сессия проверит только очевидное: соединение
поднимается, сообщение доходит. **С этого и начать**, результат записать; дальше по
факту.

### E2 — правка 11-в: токен чата в адресе

Токен в URL оседает в истории браузера, в логах прокси и в реферере. Ровно эта дыра
закрыта в потоке распознавания заменой `EventSource` на `fetch` с заголовком; у
браузерного WebSocket заголовков нет вовсе, поэтому нужен другой ход — **одноразовый
тикет**: ручка выдаёт короткоживущий код, сокет открывается по нему, код гаснет при
первом использовании.

Отдельная история с бэкендовой половиной. Сегодня чат работает — значит это долг, а не
пожар.

---

## Что делать в первую очередь

1. **Пачка A** — три правки, все дешёвые, результат виден сразу.
2. **Пачка B** — одной задачей, вместе с ревизией отступов.
3. **Пачка C** — через мокап.
4. **D1 и D2** — экранные, делать после C.
5. **D3** — история целиком, бэкенд плюс фронт.
6. **E1** — как только человек уточнит симптом; **E2** — задачей в бэклог.

## Чего в этом плане нет намеренно

- **«0 поставок» и «предоплата 0 %» на карточке витрины** — сервер не считает сделки
  поставщика и не хранит предоплату. Незаконченная история 16.
- **Пустая лента машин** — объявления удалены по просьбе человека 2026-09-05.
- **Шрифты 404** — файлы не попали в образ фронта; на поведение не влияют, консоль
  ругается на каждой странице. Задача в инфраструктуру.
