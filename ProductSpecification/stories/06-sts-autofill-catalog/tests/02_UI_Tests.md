# UI Tests — Автозаполнение из СТС: привязка к справочнику

UI = n/a.

Step 2 of the sale wizard — the recognised values marked as filled in, the linked
make/model selects, and the three recognition states on screen — is deliberately outside
this story (`interview.md`, Scope). It is carried by its own frontend story, row 20 of
`ProductSpecification/stories.md`, whose scenarios are written against the `autofill`
object this story publishes.
