# Tier 3 — записано и не строится

- Сохранность роли после перезапуска: роль — колонка в Postgres, проверяет базу.
- Гонка двух модераторов на одной заявке: единственность решения держит проверка
  статуса в той же транзакции; тест проверял бы транзакцию Postgres.
- Права каждой роли поимённо: карта прав — данные, а не поведение; ломается она
  вместе с маршрутами, которые её читают.
