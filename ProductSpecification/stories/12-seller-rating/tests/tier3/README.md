# Tier 3 — записано и не строится

- Ответ продавца на отзыв и жалоба на отзыв: модерация отзывов — отдельная история,
  правил у неё нет.
- Точность округления средней оценки до знака при десятках тысяч отзывов: проверяет
  арифметику языка, а не правило.
- Пересчёт агрегата под одновременными записями: единственность отзыва держит база,
  а гонку двух отзывов по одному офферу она же и снимает.
- Кэш профиля и его инвалидация: кэша нет, проверять нечего.
- int-01 сквозной путь принятия и оценки: разрезан по частям — api-01 проверяет запись,
  api-07 проверяет, что агрегат доехал до карточки; целиком поверх тех же двух вызовов
  проверял бы TestClient, а не правило.
- inf-02 откат ревизии рейтинга: тот же откат гоняет гейт `alembic downgrade -1 &&
  upgrade head` в полном прогоне — тест повторял бы гейт.
