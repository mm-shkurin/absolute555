# 20 — Шаг 2 мастера: распознанное, правка, три состояния

Фронтовая полоса. Бэкендовая половина здесь одна — ручка распознавания по VIN.

## Спека

- [x] interview
- [x] контракт: `POST /sale_car/{id}/decode-vin` в `api-specs/sale_car_sts.yaml`
- [x] карта контрактов: раздел «Стори 20»

## Фронт

- [x] словарь источников сведён к проводу: `manual` / `recognized`
- [x] пометка только у марки и модели — год, коробка, мощность, VIN без источника
- [x] плашка мощности: вместо выдуманного расхождения — «не подтянулась, впишите из СТС»
- [x] правка продавца снимает пометку распознанного (было в `useDraftState`, стало видимым)
- [x] «Проверить VIN» зовёт ручку и уводит в «читаем», а не молча дальше
- [x] заглушка отвечает на `/sale_car/{id}/decode-vin`; гейт паритета читает `fixtures/mutations.ts`
- [x] чистый слой: `draftWire` не помечает то, чей источник сервер не назвал
- [x] браузерный слой: пометку на шаге 2 получают ровно «Марка» и «Модель»
- [x] рефактор

## Вторая полоса

- [x] ACTION: сервер реализовал `POST /sale_car/{id}/decode-vin` (a8f9b2f)
