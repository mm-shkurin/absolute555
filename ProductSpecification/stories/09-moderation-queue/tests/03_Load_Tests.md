# Load Tests — Модерация: очередь, отклонение с причиной, жалобы

Load = n/a.

`ProductSpecification/ExpectedLoad.md`: десятки новых объявлений в день. Очередь читают
один-два модератора, жалобы приходят реже объявлений. Ни один заявленный профиль
нагрузки не задействован; страницы у очереди есть не ради объёма, а ради того, чтобы
плохая неделя не выдавала триста строк одним ответом.
